---
id: 2026-02-17
aliases: []
tags:
  - daily-notes
---

## Notes

- Spent morning looking at Yao's "bug", was really a miscommunication
- Fixed mbo_utilities bug with re-writing binary rather than just copying it
- Also took care of data-level metadata being properly overwritten e.g. "save_path"
- Outer level OME metadata not compatible with Webknossos, which requries a "multiscales" dict at root level
- Tenengrad sharpness metrics for fusion 
- Fix zarr bug for 2D files, Webknossos compatible
- Verified first array not tranformed at all, second array now just needs HFLIP (visual) VFLIP (python parameter) -> Set by default, so unless things change, these can be hidden away as optional overrides?

## IMPLEMENTED

- Per-tile cropping
- Tenengrad sharpness metric: crossover Z, blending range via (FWHM) of first derivative of tenengrad sharpness, spatial variability in sharpness per X-Strip
- Runlogs at the root directory (json), config snapshot
- correct_stack, multi_fuse
- better overwrite handling
- Zarr v3/NGFF v0.5 only
- Fix cropping taken into account for metadata image dimensions
- Fix orientation / VW0N for output filename
