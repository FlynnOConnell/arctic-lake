---
id: 2026-01-27
aliases: []
tags:
  - daily-notes
---

## Notes

- Parameter sweeps over median, percentile and background parameters are now only a few pixels off between matlab and python
- Take a closer look at the metadata, cam identification? 

Metadata (req):

- dimensions:	768x1848x38,768x1848x38	Image size (W×H×Z). Parsed to get zplanes=38
- z_step:	6.220	Physical spacing between Z-planes (micrometers)
- exposure_time:	0.7	Exposure in ms. Used to calculate fps = 1000/0.7 ≈ 1428 and vps = fps/zplanes
- detection_objective:	Zeiss 20x/1.00	Magnification extracted via regex → objective_mag=20.0

Derived:

- zplanes = 38  # from dimensions
- fps = 1000 / 0.7  # ~1428 frames/sec
- vps = fps / zplanes  # ~37.6 volumes/sec
- pixel_resolution_um = 6.5 (hardcoded) / objective_mag (20.0)  # = 0.325 µm/pixel

From older notes, shouldn't the detection objective magnification should be **16x** rather than the XML metadata value: 20x

### Segmentation Mask Workflow

1. Run clusterPT with segmentMode=2
2. Run clusterPT again to generate global masks
3. Run clusterPT a third time to apply to remaining image stacks

-> Python accomplishes this in a single run!

When would we want to use Global segmentation mode?

segment_mode=0: No segmentation/masking
segment_mode=1: Segment + apply mask to output (removes background)
segment_mode=2: Generate masks only (saves .segmentationMask.klb files, doesn't apply to output)
segment_mode=3: Apply a pre-existing global mask (reads from global_mask_dir)

Params that effect the segmentation masking:

Gaussian kernel size	gauss_kernel	Larger = more smoothing, fewer small features
Gaussian sigma	gauss_sigma	Larger = more blur, smoother boundaries
Threshold	segment_threshold	Lower = more permissive (more foreground), Higher = stricter
Mask percentile	mask_percentile	Percentile for minimum intensity calculation
To remove small "dots" in the background, you have a few options:

Increase gauss_sigma (e.g., 2.0 → 3.0) - more smoothing before thresholding
Increase segment_threshold (e.g., 0.4 → 0.5) - stricter threshold removes weak features
Add morphological cleanup - the code in masks.py has apply_bwareaopen() but it's only used in the fusion pipeline

## Cropping out side regions

i want to keep only ~400-1580 and trim the rest on either side in Y, and ~190 to 620 in X. And in Z, 5-35

# crop parameters - per camera (same values for both cam 0 and 1)
crop_top=[400, 400],      # y start (removes 0-399)
crop_height=[1180, 1180], # y size (400 to 1580 = 1180 pixels)
crop_left=[190, 190],     # x start (removes 0-189)
crop_width=[430, 430],    # x size (190 to 620 = 430 pixels)
crop_front=[5, 5],        # z start (removes 0-4)
crop_depth=[30, 30],      # z size (5 to 35 = 30 slices)

- subdir = "py-s2-gs3-cropped-v2": cropping
- subdir = "py-s2-gs3-cropped-v3": fixing the crops (30->35 crop_depth)

