---
tags: [weekly, meeting, log]
template: Weekly Meetings
date: 2026-03-19
---

# Weekly Meeting – March 16 - March 20

## (previous) TO-DO
*from [[2026-W11]]*
- conjugation of all BDV affine matrices into a single matrix; figure out row-major vs column-major versions
- diSPIMFusion Deconvolution with PSFs (to be shared), Richardson Lucy iter=10, 20, 40 for comparison
- PSF measurement (FWHM): Beads dataset with Z-sampling matched to XY sampling
to do: average bead estimate for each view (avoid saturated ones and clumps), then run bead-analyzer, https://github.com/natearnas/bead-analyzer, to estimate PSF

df/f related
- Drosophila 2 Hz dataset, VW00 and VW90
- for each view, segment neurons then df/f using SiMView pipeline (MATLAB)
- cellpose-SAM, suite2p
- compare outputs of a and bead

## Main Projects

#### IsoView

- Registration: BDV single matrix: multiple outermost -> innermost 
    - `T_full = T_registration @ T_manual @ T_calibration`
    - Python (scipy): transpose XYZ to ZYX, invert
    - MATLAB (affine3d): transpose rotation block, move translation from column 4 to row 4
    - diSPIMFusion: Scale to isotropic
- Deconvolution: PSF's must be in the same orientation as input data
    - No single-view or duel-view deconvolution results that I would interpret as improvements relative to non-fused views
    - Provided [0.8x0.8x4.0] psf's produced the best result, sharper in some regions, might be signal loss in neuronally-dense regions
        - Born-Wolf PSF likely too large, too much signal removed
    - Used `regdeconvproject`, due to `dispimfusion` requiring anisotropic input volumes (our data are isotropic due to being pre-registered)
        -   same RL algorithm, different implementations (MATLAB fft vs CUDA)
    - From what [I've read](https://forum.microlist.org/t/psf-of-spim-systems-and-deconvolution/1346) (PSF of SPIM systems and deconvolution), PSF needs to be taken in the exact same imaging conditions as the day acquisition happens
- DF/F: Initial run to test code
    - Ran VW00, with claude-produced fiji macro to load it as virtual stack (claude is really nice for fiji macros)
    - Where to store outputs?


## Collaborations

- Initiated `mbo-columbia-collab` slack channel
- MaskNMF: 
    - Updated dataset descriptions in slack (date, animal name, experimentor)
    - Ran masknmf without the temporal denoiser, neuro-wobbles are back (will post in slack)

## Data: `MBO_DATA`

- Media: 

## Research Papers / Packages

- PetaKit5D: Unified, scalable petabyte scale light-sheet processing
    - Goal is to alleviate extensive, multi-step workflows (which we currently have)
    - Deconvolution (RL) on raw data is faster with less edge artifacts
    - Optimizations: combine deskew + rotation into a single step + deconvolve before making isotropic
        - We don't deskew (stage-scanning mode only), but we could deconvolve individual views before rotation/translation
    - Custom C++ Zarr v2 (and tiff) writers: negative of doing this is it's going to be a nightmare if they want Zarr v3
        - compared to tensorstore: 2.2x faster read 1.5x faster write

## Misc

- compare_line_profiles.py script for comparing deconvolution results
- view_dff.ij script for loading DF/F timepoint (as volume) in Fiji
- Ignore pixel->um in OME-TIFF Pyramids for now
- Hands on compute sessions (2-3 hours/session, 3+ sessions or so)
    - Python environments, navigating the terminal (Windows vs Linux)
    - File formats, Tiff vs Zarr vs HDF5 in MATLAB, Python

## (Next) TO-DO

*for [[2026-W13]]*

- [ ] (~1 day) Registration solution without requiring manual bigstitcher (compare with NCC, as in regDeconvProject publication)
- [ ] (~1 day) Beads: avg bead estimate using bead-analyzer to estimate PSF
- [ ] IsoView-Processing DF/F, Segment neurons with Cellpose
