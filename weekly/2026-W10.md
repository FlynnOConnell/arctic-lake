---
tags: [weekly, meeting, log]
template: Weekly Meetings
date: 2026-03-05
---

# Weekly Meeting – March 02 - March 06, 2026

## (previous) TO-DO
*from [[2026-W09]]*
1. Normal vs Rotated Cameras, CM2->CM0 alignment and registration
2. Towards `correct_stack` as fully reversable/lossless
3. Finish validating and push mbo_utilities v2.7.0 with multi-processing / threading improvements
4. Isoview multi-processing and optimization

## Main Projects

#### IsoView
- Toward fully reversable `correct_stack`
  - Dead-pixel correction is not currently reversable
      - pixels that are outside std/mean range overwritten with median-filtered values, originals not stored
      - fix: save original values at flagged (x,y,z) locations before overwriting, e.g. as a sparse array
  - Foreground segmentation zeros out background voxels via binary mask multiplication
      - `multi_fuse` already generates masks if none exist, could change `segment_mode` in `correct_stack` to instead not apply masks, just optionally generate
- `backgroundValues` in `clusterPT` is not image background subtraction
  - scalar per-camera dark offset, used only inside dead-pixel detection to normalize the mean projection before computing relative distances
  - actual background subtraction happens later in `clusterFR` / `filterResults.m` ("adaptive image background correction")
- Normal vs Rotated Camera
    - No special handling needed for `correct_stack` / `multi_fuse`, will be needed for step 3: `fuse_views`
    - CM02->CM00: (Z,Y,X) → (X,Y,Z_flipped)
    - ImageJ/FIJI = `Reslice (TOP)` + `Flip_Vertical` -> `Reslice (LEFT)`
    - Python: `np.flip(volume, axis=0).transpose(2, 1, 0)`
- Implemented basic multi-processing: setting up a worker for each timepoint/tile
- `isoview_config.json`: root level, stores parameters and timings for each processing step

#### mbo_utilities
- Fixed Suite3D multi-processing bug

## Misc
- Denied from the Janelia deep-learning workshop
- I'd prefer moving desk back downstairs somewhat soon

## (Next) TO-DO
*for [[2026-W11]]*
- [ ]
