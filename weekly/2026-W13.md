---
tags: [weekly, meeting, log]
template: Weekly Meetings
date: <% tp.date.now("YYYY-MM-DD") %>
---

# Weekly Meeting – <% tp.date.now("MMMM DD") %> - <% tp.date.now("MMMM DD, YYYY", 6) %>

## (previous) TO-DO
*from [[<% tp.date.now("YYYY-[W]WW", -7) %>]]*
- Registration solution without requiring manual bigstitcher (compare with NCC, as in regDeconvProject publication)
- Beads: avg bead estimate using bead-analyzer to estimate PSF
- IsoView-Processing TimeFuse, correctStack, DF/F, Segment neurons with Suite2p/Cellpose, compare with manual imageJ segmentation on MATLAB DF/F

## Main Projects

### mbo_utilities

- Miller Brain Suite: Updated README.md, GUI documentation
- StyletE: PiezoAveraging caused bug in z-stats
- Fixed Set-metadata GUI bug, made better UI for setting metadata (tabs rather than buttons), you need to set metadata for save-as and processing (suite2p) so its tricky

### IsoView

- Richardson-Lucy 10 Iterations vs Weiner-butterworth 1 iteration
    - Near-equivalent deconvolution, 149s (RL) -> 42s (WB)
- Deconvolution improvements are more obvious when camera is rotated into its sharpest orientation
- PSF Analysis:
  - Measured FWHM from 0p8-0p8-8p0_cm00-psf.tif 1.97
  - Bead-analyzer on fused + per-camera corrected data (blob detection, 3-8 beads per run)
      - XY FWHM: 2.0-2.9 um (1D gauss), Z FWHM: 6.0-8.5 um (undersampled at 6.2 um z-step)
      - 3D gauss gives tighter Z estimates (~6-7 um) but still sampling-limited
      - cameras within pairs consistent (CM00~CM01, CM02~CM03), fusion doesn't degrade lateral
      - trackpy WIP: detected 153 beads at minmass=500 (too many), needs minmass~2000-3000
      - avg bead: 20 beads nearest median Z-FWHM, 4x upsample, center, average
  - XY FWHM higher than expected (~2.1-2.6 vs ~1.6 um), is Z measurement unreliable without finer z-step?
  - Full results: `Y...\2026-01-23_drosophila\psf_analyzer\`
- Full MATLAB run (after correct_stack), see `processing_log.md`
- `extract_planes.py`: make `TYX` planar timeseries for use with Suite2p / Cellpose
- Registration with Suite2p -> Cellpose, segments peripheral beads rather than neural signal
    - Need to verify registration
- `fuse_views.py`, same code used in cam-cam fusion with averaging only
- Full MATLAB pipeline run (61 timepoints, 2 views) through all stages:
  - `clusterPT` (corrected) -> `clusterMF` (multiFuse) -> `localAP` (smoothed transforms) -> `clusterTF` (timeFuse) -> `localEC`/`clusterCS` (drift + intensity correction) -> `localCR`/`clusterCD` (baseline + dF/F)
  - `clusterFR` (background subtraction) run separately for comparison
    - subtraction was too agressive? Need to check this more carefully
  - Created `_vw` parameter scripts for 2-camera fusion (processingMode=2)
  - Fixed parameter mismatches vs Python pipeline: maskFactor (0.5->1.0), maskFusion (intersection->union), flipHFlag (off->on), fusionType mapping differences between
multiFuse/timeFuse
  - Reran with corrected params in `matlab2/` for comparison
  - See `processing_log.md` for full folder/output documentation
- Ran Suite2p (LBM-Suite2p-Python) on planar timeseries
  - nonrigid registration enabled for belly-roll motion
  - segments peripheral beads rather than neural signal, needs parameter tuning (diameter, threshold_scaling)
- `extract_planes.py`: per-z-plane `TYX` and single-file `TZYX` outputs for Suite2p/Cellpose
- `fuse_bdv.py` script to fuse multi-tile dataset.xml into single OME-TIFF for bigstitcher
- Fusion in bigstitcher failed even after raising max-memory to 95k MB

## Collaborations

- masknmf: waiting on Amol to make some more user-friendly curation / exploration code

## Misc

-  Stephen Saalfel suggested File IO addition to fastplotlib, noted our last meeting where we discussed keeping collaborative code in our repositories rather than working in fastplotlib. Prime example of this. I should not be writing or maintaining any I/O fastplotlib code.
    - "ImageJ for system neuro" ... "actually not ImageJ, more like big data viewer"
- Future Workshop 
    - Env ecosystem: pip, pip-env, pipx, anaconda, conda, mamba, miniconda, miniforge, micromamba

## (Next) TO-DO
*for [[<% tp.date.now("YYYY-[W]WW", 7) %>]]*
(WIP)
- Registration solution without requiring manual bigstitcher (compare with NCC, as in regDeconvProject publication)
- Beads: avg bead estimate using bead-analyzer to estimate PSF
- IsoView-Processing TimeFuse, correctStack, DF/F, Segment neurons with Suite2p/Cellpose, compare with manual imageJ segmentation on MATLAB DF/F
