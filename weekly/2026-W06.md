---
tags: [weekly, meeting, log]
template: Weekly Meetings
date: 2026-02-05
---

# Weekly Meeting – February 02 - February 06

## (previous) TO-DO
*from [[2026-W05]]*
- [x] Implement cropping for Python
- [ ] Continue implementing `multi_fuse.py`
- [ ] Implement Zarr v3 for step 1: process-timepoint

## Weekly Overview
Focused on clusterPT, which is bug-free but needs some housekeeping.

MultiFuse has intensity bugs causing spikes not present in clusterPT, during both geometric and adaptive blending. 

## Main Projects

#### mbo_utilities

- Channel selection for non-LBM datasets
- Yao reported a bug that I cannot reproduce, advised to update to the most recent version and try to reproduce the bug.

#### IsoView

- ClusterPT (python) needs some housekeeping (see Feb 4th note for whats missing from MATLAB)
- clusterMF (python) has intensity bugs that need to be corrected
- Directory structure: Root/data-name/SPM00/TM0000N, Root/data-name.corrected/SPM00/TM0000N, etc
- Both clusterPT and clusterMF need to be compatible with arrays that differ in shape, use padding on the smaller array
- ClusterPT cropping needs to be validated
- After clusterMF bugfix -> histograms/z-projection match, ensure Geometric and Adpative blending are properly ported from MATLAB to Python
- Zarr v2 or v3, if v2 note that v3 needs to be implemented

## Compute/Storage Server Usage

- `S1_DATA` is for MBO Users, `foconnell` should really not exist
- Move exploration folders to mbospace, decide which belong in Projects
- Ensure everything follows the correct structure, date-tag-description when necessary

## (Next) TO-DO

*for [[2026-W07]]*

- [ ] clusterPT directory structure and cropping housekeeping
- [ ] clusterMF bugfixes, confirm geometric/adaptive blending
- [ ] Zarr support in both clusterPT and clusterMF
- [ ] Update storage server with results that have proper directory structure

